# Claude Code — machine-local overrides, never shared
.claude/settings.local.json
.claude/*.local.json

# docker compose env — real credentials, never shared
.env

# playwright / browser-automation artifacts
.playwright-mcp/
test-results/
playwright-report/

# OS / editor noise
.DS_Store
Thumbs.db
*.log
